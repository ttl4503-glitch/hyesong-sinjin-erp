# 공사현황관리 ERP — 개발 기획서

## 배경
혜송산업개발(주), 신진조경(주) 두 회사의 공사 진행 현황과 인력·장비·자재 투입을 관리하는 웹앱.
현재 Claude.ai에서 브라우저 저장 방식 프로토타입으로 기능을 검증한 상태이며,
이제 실제 서버·로그인이 붙은 다중 사용자 앱으로 만들려고 함.

## 사용자
- 대표(전체 열람/관리)
- 사무실 직원 (데이터 입력·조회)
- (추후) 현장소장 등 현장 인력 — 모바일 중심 사용

## 사용 환경
- PC(사무실)와 모바일(현장) 양쪽에서 동일하게 사용
- 반응형 웹앱(PWA) 권장 — 모바일은 홈화면 설치, PC는 브라우저 접속

## 핵심 요구사항

### 1. 로그인 / 권한
- 계정 기반 로그인 (이메일+비밀번호, 또는 소셜 로그인)
- 역할 구분: 관리자(전체 접근) / 일반 직원(입력·조회)
- 회사(혜송산업개발 / 신진조경) 단위로 데이터는 구분해서 보여주되, 한 시스템 내에서 관리

### 2. 공사(프로젝트) 관리
- 필드: 회사, 공사명, 현장 위치, 착공일, 준공(예정)일, 계약금액, 메모
- 세부 일정(마일스톤): 이름, 날짜, 완료 체크
- 공정률: **투입비용 기준 자동계산** = 누적 투입비용 ÷ 계약금액 × 100
  (계약금액 미입력 시 수동 슬라이더로 대체)

### 3. 인력·장비·자재·식대·잡자재 투입관리
- 구분(5종): 인력 / 장비 / 자재(반입) / 식대 / 잡자재
- 인력: 직종(조경공/보통인부/잔디공 등 자유 입력+자동완성) + 이름(자동완성, 과거 입력 이력 재사용) + 공수(0.5 단위) + 단가 → 금액 자동계산(공수×단가)
- 장비: 이름 + 공수 + 단가 → 금액 자동계산
- 자재/잡자재: 항목명 + 수량/단위(자유) + 금액 + **세금계산서 발행여부(발행/미발행) 체크**
- 식대: 항목명 + 금액
- 모든 투입 기록: 수정(단가·공수 등 사후 변경 가능) / 삭제 가능
- 오늘 날짜 기준 회사 전체 투입 현황(인력 공수, 장비 공수, 비용 합계) 대시보드 표시

### 4. 착공내역서(공종별 예산) & 공종별 진행률
- 엑셀(xlsx) 업로드로 공종별 예산 항목 일괄 등록 (공종명, 규격, 수량, 단위, 단가, 금액)
- 일반적인 내역서 컬럼명 자동 인식: 공종/품명, 규격, 단위, 수량, 단가, 금액(다양한 표기 허용)
- 인력·장비·자재 투입 등록 시 "공종" 선택 가능 (어느 공종에 들어간 비용인지 태깅)
- 공종별 진행률 = 해당 공종에 태깅된 누적 투입금액 ÷ 해당 공종 예산 × 100 (진행바 표시)

### 5. 리포트
- 인력·장비 투입 데이터를 **월별 × 현장별** 집계하여 엑셀(xlsx) 다운로드
  - 월 → 현장별 소계 → 월별 소계 → 전체 총합계 구조
  - 컬럼: 월, 현장명, 인력 공수, 인력 비용, 장비 공수, 장비 비용, 합계 비용

## 데이터 모델 (참고용, 현재 프로토타입 기준)
```
Project {
  id, company, name, location, startDate, endDate, contractAmount, progress, memo,
  milestones: [{ id, title, date, done }],
  laborLogs: [{ id, type, jobType, name, qty, unit, rate, amount, date, note, taxInvoice, workItemId }],
  workItems: [{ id, name, spec, unit, qty, unitPrice, amount }],
  workItemsTotal, workItemsFileName
}
```

## 기술 스택 제안
- 프론트: 반응형 웹앱(PWA), 기존 프로토타입 UI/UX 최대한 재사용
- 백엔드: Node.js/Express 또는 유사 프레임워크 + DB(PostgreSQL 등)
- 인증: 이메일/비밀번호 + 세션 또는 JWT, 역할 기반 접근제어(RBAC)
- 배포: 소규모 사내용이므로 가벼운 클라우드 서버 1대로 충분

## 참고
현재 프로토타입 파일: `공사현황관리.html` (Claude.ai 대화에서 생성, 브라우저 저장 방식)
이 문서와 함께 Claude Code에 프로토타입 파일을 참고자료로 첨부하면 UI/기능을 그대로 재현하며 서버 버전으로 전환하기 수월합니다.
